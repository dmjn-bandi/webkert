import { TimeRangeFormatPipe } from './time-range-format.pipe';

describe('TimeRangeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeRangeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
